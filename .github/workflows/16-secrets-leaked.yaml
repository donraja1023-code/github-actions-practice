name: Secrets Leaked

on:
  workflow_dispatch:

jobs:
  leaked:
    runs-on: ubuntu-latest
    steps:
    - name: Leak Secrets
      env:
        PROD_TOKEN: "${{ secrets.PRODUCTION_TOKEN }}"
      run: |
        echo "${{ secrets.PRODUCTION_TOKEN }}" | base64
        echo "$PROD_TOKEN" | base64